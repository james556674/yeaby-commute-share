import styled from "styled-components"

const MainContainer = styled.div`
  height: 100%;
  width: 100%;
  overflow: hidden;
  position: fixed;
`

const Layout = () => {
  return (
    <MainContainer>
      Hello World
    </MainContainer>
  )
}

export default Layout